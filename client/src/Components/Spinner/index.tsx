import React from 'react'
import { Loader, LoaderWrapper } from './style';

const Spinner = () => {
    return (
        <LoaderWrapper>
            <Loader />
        </LoaderWrapper>
    )
}

export default Spinner